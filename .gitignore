out/
toolchain/
*.pyc
__pycache__/
.mojo_cache/
build/
build
ramulator.stats

# Prebuilt LLVM release asset fetched by image.yml (see Dockerfile)
llvm-asset/

# Prebuilt detour release asset fetched by image.yml (see Dockerfile)
detour-asset/
